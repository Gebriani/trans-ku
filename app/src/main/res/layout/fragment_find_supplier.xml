<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/ac_find_supp_root_search"
        layout="@layout/item_find_supplier_cardview" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/ac_find_supp_recycleview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/ac_find_supp_root_search"/>

    <ProgressBar
        android:id="@+id/ac_find_supp_progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

</RelativeLayout>